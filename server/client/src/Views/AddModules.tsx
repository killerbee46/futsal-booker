const AddModules = () => {
  const handleAddModule = () => {
    alert('Added Module');
  };
  return <button onClick={handleAddModule}>Add transaction module</button>;
};

export default AddModules;
